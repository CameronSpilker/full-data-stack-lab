{
  "name": "dashboard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "sources": "evidence sources && node scripts/write-empty-sources.mjs",
    "dev": "evidence dev",
    "build": "evidence build",
    "preview": "evidence preview",
    "fetch": "bash scripts/fetch-warehouse.sh",
    "build:deploy": "npm run fetch && npm run sources && npm run build"
  },
  "dependencies": {
    "@duckdb/node-api": "^1.4.2-r.1",
    "@evidence-dev/core-components": "^5.0.5",
    "@evidence-dev/duckdb": "^2.0.1",
    "@evidence-dev/evidence": "^40.1.8"
  },
  "devDependencies": {
    "typescript": "5.4.2"
  }
}
